@model IEnumerable<GulfCoastWorkforceSolutions.Models.Orders.OrdersListViewModel>

@{
    ViewBag.Title = @ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourOrders.Orders");
}

<a href="@Url.Action("YourAccount", "Account")">&larr; @ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourAccount.Back")</a>


<h2>@ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourOrders.Orders")</h2>

<div id="no-more-tables">
    <table class="orders-table">
        <thead>
            <tr>
                <th class="column-id">@ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourOrders.OrderId")</th>
                <th class="column-date">@ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourOrders.Date")</th>
                <th class="column-status">@ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourOrders.Status")</th>
                <th class="column-total">@ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourOrders.Total")</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model)
            {
                <tr>
                    <td class="column-id">
                        <span class="strong">@order.OrderInvoiceNumber</span>
                        @Html.ActionLink(ResHelper.GetString("GulfCoastWorkforceSolutionsMvc.YourOrders.OrderDetail"), "OrderDetail", new { orderID = order.OrderID }, new { @class = "btn btn-terciary" })
                    </td>
                    <td class="column-date">@order.OrderDate</td>
                    <td class="column-status">
                        <span class="status">
                            @ResHelper.LocalizeString(order.StatusName)
                        </span>
                    </td>
                    <td class="column-total">@order.FormattedTotalPrice</td>
                </tr>
            }
        </tbody>
    </table>
</div>

